# Emerging Runtime Research

These are monitored theoretical candidates, not automatically independent projects or new Runtime Foundations.

## Operational Realization Theory

Candidate core:

- commitment;
- admission;
- binding;
- realization;
- support/discharge;
- effects/evidence;
- standing;
- reconciliation;
- admissible continuation.

Current disposition: emerging theoretical core with Runtime as primary specialization/consumer. Promote to an independent owner only if multiple autonomous domains require the theory while Runtime ownership becomes an artificial constraint.

## Standing / Currentness Semantics

Candidate core:

- historical truth vs current validity;
- current authority;
- current usability;
- supersession;
- retirement;
- reclaimability;
- validity domains and fencing.

Current disposition: high-value cross-owner bridge because similar structures appear in Runtime, Host/Research recovery, Network currentness and durable provenance.

## Operational Claim Theory

Candidate compression:

`OperationalClaim = Identity + Scope + Support + Standing`.

Current disposition: epistemic layer of Operational Realization Theory. Do not split into a separate project without independent falsification pressure.

## Computational economy bridge

C3 confirms Fresh-Execution Non-Necessity for identity-preserving durable replay, but also shows that current Runtime does not infer generic cross-Operation reuse merely from equal mechanical `operationDigest` values.

The earlier shorthand `ComputeNeed = UnsatisfiedSupportGap` is therefore narrowed to the bounded hypothesis:

`FreshExecutionNeed = UnsatisfiedAdmissibleSupportGap`

where admissible support must already have sufficient identity/binding/input compatibility, evidence scope and current standing for the obligation. This is not yet a universal law or a generic production Runtime reuse mechanism.
